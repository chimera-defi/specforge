#!/bin/sh
branch=$(git symbolic-ref --quiet --short HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "ERROR: Direct pushes to $branch are blocked. Use a PR branch." >&2
  exit 1
fi
exit 0
